import { FirstAlphabetToUpperPipe } from './first-alphabet-to-upper.pipe';

describe('FirstAlphabetToUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstAlphabetToUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
